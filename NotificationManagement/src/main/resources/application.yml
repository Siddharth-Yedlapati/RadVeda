server:
  port: 9193
spring:
  datasource:
    url: jdbc:mysql://localhost:4000/NotificationManagementDB
    username: root
    password: mqpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
