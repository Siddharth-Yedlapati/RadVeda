server:
  port: 9203
spring:
  datasource:
    url: jdbc:mysql://localhost:4000/AnalyticsDB
    username: root
    password: mqpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true