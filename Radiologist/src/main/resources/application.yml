server:
  port: 9201
spring:
  datasource:
    url: jdbc:mysql://localhost:4000/RadiologistServiceDB
    username: root
    password: mqpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
